services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    env_file:
      - .env.local
    command: npm run dev
volumes:
  node_modules:
